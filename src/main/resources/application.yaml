cloud:
  aws:
    credentials:
      access-key:
      secret-key:
    region:
      static: # region example: us-east-2
    stack:
      auto: false

application:
  bucket:
    name: # S3 bucket name
  topic:
    name: # topic ARN


spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB

server:
  port: 9090
